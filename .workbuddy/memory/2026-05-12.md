# 2026-05-12 工作日志

## 修复 build.js ReferenceError: formatDate is not defined
- 问题：build.js第205行调用 `formatDate(r.mtime)`，但该函数未定义
- 修复：在 parseDateStr 后新增 formatDate(date) 函数，将 Date 对象转为 YYYY.MM.DD 格式
- 构建运行成功：19份报告 → 去重后17份独立报告 → index.html 生成

## 修复 extractReportDate 无法识别 HTML实体编码的中文日期标签
- 问题：LVMUY 报告日期显示为"[日期未知]"，实际文件内有"分析日期：2026-05-12"和"报告生成时间：2026-05-12"
- 根因：该报告HTML文件中中文使用HTML实体编码（&#xHHHH;），而 extractReportDate 的 regex 搜索原始中文字符，全部匹配不上
- 修复：新增 decodeHtmlEntities 函数，在 extractReportDate 和 extractDescription 中先解码实体再运行 regex
- 验证：LVMUY 正确显示 [2026.05.12]
