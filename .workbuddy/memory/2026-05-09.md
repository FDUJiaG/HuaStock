# 工作记忆 - 2026-05-09

## 研究报告管理页面搭建

- 创建了 `build.js`（自研静态站点生成器，零外部依赖）：递归扫描 Stocks/ 目录，提取 HTML 报告 `<title>` 标签和修改时间，按 A-Shares/HK-Stocks/US-Stocks 子文件夹分类
- 生成了 `index.html`：深蓝+金色专业研究风格，含分类过滤（全部/A股/港股/美股）、实时搜索、卡片动画，使用 Tailwind CSS + Noto Serif SC 字体
- 创建了 `config.json`：站点配置和命名规范说明
- 创建了 `package.json`：包含 `node build.js` 构建命令
- 创建了 `.github/workflows/pages.yml`：GitHub Actions 自动构建并部署到 GitHub Pages
- 更新了 `README.md`：完整的使用流程、目录结构、文件命名规范和构建原理
- **命名规范建议**（不改已有文件）：`{公司名}-{代码}-{报告类型}.html`（如 `宁德时代-300750-深度研究.html`）
- **使用流程**：在 Stocks/ 分类目录添加 HTML → `node build.js` → git push（自动部署）
- **标题去尾缀**：build.js 新增 `cleanTitle()` 函数，自动去除 `<title>` 末尾的日期/时间尾缀（如 `— 2026年4月`），因日期已单独展示在卡片上
- **报告日期优化**：build.js 新增 `extractReportDate()` 函数，从每篇报告 HTML 中提取实际报告日期（支持 `<div class="date">报告日期：2026年5月8日</div>`、`分析日期：YYYY-MM-DD`、`<meta name="date">`、`YYYY年M月D日` 等多种格式），卡片上显示报告日期而非文件修改时间，并按报告日期排序
- **favicon 集成**：左上角图标和浏览器标签页图标使用 `favicon.jpg`
- **README 更新**：同步最新目录结构（含实际报告文件和 favicon），构建说明补充日期提取、标题净化等新特性
- **跨文件夹去重**：build.js 支持同名文件跨文件夹去重——同一份报告放入 A-Shares 和 HK-Stocks 时合并为一条记录，卡片同时显示"A股+港股"双标签，在各分类选项卡下均可看到；总数去重统计，分类计数按文件夹原始分布统计
- **HKEX_vs_CME 报告修复**：修复第一章 CME 概览框缺少 `</table>` 闭合标签导致的 HTML 结构错乱；删除第四章（商业模式）内部重复的「核心财务指标对比」子表；重排章节顺序为 概览→商业模式→Q1业绩→核心财务指标；更新章节编号
- **返回首页按钮**：build.js 新增 `injectBackButton()` 函数，自动向每份报告 HTML 注入右下角浮动「← 返回首页」按钮，链接路径根据文件深度自动计算，幂等（重复构建不会重复注入），深蓝半透明磨砂玻璃风格 + 金色点缀 + 悬停上浮效果
